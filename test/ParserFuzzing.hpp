#include "Common.hpp"

namespace ParserFuzzing
{

  /*
void genToken(ostream& os)
{
  //double prob = (double) rand() / RAND_MAX;
}

string genRandomFile(size_t len)
{
  string fname = "parsefuzz.os";
  return fname;
}
*/

}

